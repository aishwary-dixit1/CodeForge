# PostgreSQL connection string
# Example: postgresql://postgres:postgres@localhost:5432/codeforge
# DOCKER: docker run -d --name codeforge-postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=codeforge -p 5432:5432 postgres:16
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/codeforge

# RabbitMQ connection string
# Example: amqp://localhost
# DOCKER: docker run -d --name codeforge-rabbit -p 5672:5672 -p 15672:15672 rabbitmq:3-management
RABBITMQ_URL=amqp://localhost

# Queue name used by API and workers
EXECUTION_QUEUE=execution.queue

# Redis for hot polling results cache
# DOCKER: docker run -d --name codeforge-redis -p 6379:6379 redis:7
REDIS_URL=redis://127.0.0.1:6379
RESULT_TTL_SECONDS=300
WORKER_HEARTBEAT_SECONDS=30
WORKER_ID=worker-local

# Sweep batch size for pending requeue job
REQUEUE_BATCH_SIZE=100

# Execution engine limits
EXEC_MEMORY_LIMIT=256m
EXEC_CPU_LIMIT=0.5
EXEC_PIDS_LIMIT=64
EXEC_TMPFS=/tmp:size=32m,noexec,nodev,nosuid

# Optional language runner image overrides
IMAGE_PYTHON3=codeforge/runner-python3:latest
IMAGE_NODEJS20=codeforge/runner-nodejs20:latest
IMAGE_JAVA17=codeforge/runner-java17:latest
IMAGE_CPP20=codeforge/runner-cpp20:latest

# smoke test inputs
PHASE2_LANG=python3
PHASE2_CODE=print('hello')
PHASE2_STDIN=
PHASE2_EXPECTED=hello
PHASE2_TIMEOUT=10

# DLQ (Dead Letter Queue) Configuration
MAX_RETRIES=3
DLQ_POLL_INTERVAL_MS=10000

# Heartbeat Monitor Configuration
WORKER_HEARTBEAT_TIMEOUT_MS=60000
HEARTBEAT_CHECK_INTERVAL_MS=15000

# Frontend URL for CORS and callback references
FRONTEND_URL=http://localhost:3001


# Autoscaler Configuration
AUTOSCALE_INTERVAL_MS=30000
TARGET_QUEUE_DEPTH=10
MIN_WORKERS=2
MAX_WORKERS=10
SCALE_UP_THRESHOLD=15
SCALE_DOWN_THRESHOLD=2
